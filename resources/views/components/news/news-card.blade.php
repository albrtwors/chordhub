        <div class="d-flex flex-column gap-1">
            <div>
                <button class="btn btn-primary">
                    {{ $version }}
                </button>
            </div>

            <h6 class="fw-bold">{{ $title }}</h6>
            {{ $desc }}
        </div>
